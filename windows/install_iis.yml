---
- name: Instalar IIS no Windows
  hosts: windows

  tasks:
    - name: Instalar o IIS
      ansible.windows.win_feature:
        name: Web-Server
        state: present
        include_management_tools: true
